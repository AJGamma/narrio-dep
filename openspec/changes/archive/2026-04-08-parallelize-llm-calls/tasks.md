## 1. 核心实现

- [x] 1.1 在 experiment.py 中新增 `--render-workers` 参数解析
- [x] 1.2 修改 run_render_stage 函数支持并发执行
- [x] 1.3 实现两阶段渲染：封面图串先生成，其余页并发
- [x] 1.4 使用 ThreadPoolExecutor 管理并发 worker
- [x] 1.5 默认并发数 = min(剩余页数，5)，5 为上限

## 2. 进度显示与日志

- [x] 2.1 添加并行任务开始时的进度提示
- [x] 2.2 每页完成时显示 [X/N] 进度和耗时
- [x] 2.3 确保日志输出包含页码标识

## 3. 错误处理

- [x] 3.1 单页失败不阻塞其他页面
- [x] 3.2 记录失败页面的错误信息
- [x] 3.3 完成后输出成功/失败统计

## 4. CLI 参数集成

- [x] 4.1 在 run 命令中添加 `--render-workers` 选项
- [x] 4.2 在 resume 命令中同步支持该参数
- [x] 4.3 更新 lab 交互模式支持并发数选择

## 5. 测试与验证

- [x] 5.1 验证默认并发数 (3 workers) 正常工作
- [x] 5.2 验证自定义并发数生效
- [x] 5.3 验证单页内容不启动并发
- [x] 5.4 验证错误隔离机制
